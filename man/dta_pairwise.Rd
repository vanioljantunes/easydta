% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{dta_pairwise}
\alias{dta_pairwise}
\title{One-call pairwise DTA meta-analysis (paired wide design)}
\usage{
dta_pairwise(
  data,
  author = "author",
  year = "year",
  intervention = "Intervention",
  control = "Control",
  tp.e = "TP.e",
  fp.e = "FP.e",
  fn.e = "FN.e",
  tn.e = "TN.e",
  tp.c = "TP.c",
  fp.c = "FP.c",
  fn.c = "FN.c",
  tn.c = "TN.c",
  studlab = NULL,
  test_var = "test",
  variance = c("equal", "unequal"),
  nAGQ = 1L,
  conf = 0.95
)
}
\arguments{
\item{data}{Data frame with one row per study.}

\item{author}{Column name of the first-author label.}

\item{year}{Column name of the publication year.}

\item{intervention}{Label to assign to the \code{.e} rows (e.g. "CT").}

\item{control}{Label to assign to the \code{.c} rows (e.g. "MRI").}

\item{tp.e, fp.e, fn.e, tn.e}{Experimental-arm 2x2 column names.}

\item{tp.c, fp.c, fn.c, tn.c}{Control-arm 2x2 column names.}

\item{studlab}{Optional: column to use as the study label instead
of \code{paste(author, year)}.  If NULL (default), the studlab is built as
\code{paste(data[[author]], data[[year]])}.}

\item{test_var}{Name for the stacked test-type column (default
\code{"test"}).  Pass this same name to \code{dta_fit_pairwise(test_var = ...)}.}

\item{variance}{Random-effects structure for the comparison: \code{"equal"}
(default, model B) or \code{"unequal"} (model E). See \code{\link[=dta_fit_pairwise]{dta_fit_pairwise()}}.}

\item{nAGQ}{Integer; Laplace by default (passed to both fitters).}

\item{conf}{Confidence level used by per-arm heterogeneity summaries
and by \code{dta_compare()} (default 0.95).}
}
\value{
An S3 object of class \code{"dta_pairwise_result"} with components:
\code{long}, \code{pair} (the \code{dta_pairwise} fit), \code{compare} (the \code{dta_compare}
result), \code{arms} (named list of two \code{dta_single} fits), and \code{labels}
(the intervention / control labels).
}
\description{
Takes a single wide data frame -- one row per study, with \code{.e}
(intervention / index) and \code{.c} (control / comparator) suffixed count
columns -- as used for paired head-to-head designs where each study
evaluated BOTH tests (e.g. the \code{schuetz} CT vs MRI data).  Runs the full
pipeline:
\enumerate{
\item \code{dta_reshape_pairwise()} -> long format (two arms stacked, shared
\code{studlab} so the within-study pairing is captured by the random effect)
\item \code{dta_fit_pairwise()}     -> nested LR-test models
\item \code{dta_compare()}          -> LR tests + Se/Sp differences
\item \code{dta_fit_single()} per arm, keyed by the \code{intervention} / \code{control}
labels, ready for \code{dta_forest()} and \code{dta_sroc()}.
}
}
\examples{
data(schuetz)
res <- dta_pairwise(schuetz, studlab = "studlab",
                    intervention = "CT", control = "MRI")
print(res)
}
