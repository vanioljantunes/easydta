% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{dta_compare_tests}
\alias{dta_compare_tests}
\title{One-call test-comparison meta-analysis (covariate / unpaired design)}
\usage{
dta_compare_tests(
  data,
  test_var,
  studlab = "studlab",
  tp = "TP",
  fp = "FP",
  fn = "FN",
  tn = "TN",
  variance = c("equal", "unequal"),
  nAGQ = 1L,
  conf = 0.95
)
}
\arguments{
\item{data}{Data frame, one row per study, with a 2x2 table and a
two-level test-type column.}

\item{test_var}{Name (string) of the test-type column.}

\item{studlab}{Column name of the study label (default \code{"studlab"}).}

\item{tp, fp, fn, tn}{Column names for the 2x2 counts.}

\item{variance}{Random-effects structure for the comparison: \code{"equal"}
(default, model B) or \code{"unequal"} (model E). See \code{\link[=dta_fit_pairwise]{dta_fit_pairwise()}}.}

\item{nAGQ}{Integer; Laplace by default.}

\item{conf}{Confidence level (default 0.95).}
}
\value{
An S3 object of class \code{"dta_pairwise_result"} (same shape as
the return of \code{dta_pairwise()}).
}
\description{
Sister of \code{\link[=dta_pairwise]{dta_pairwise()}} for the case where two diagnostic tests are
compared via a between-study covariate (Cochrane Handbook chapter 10 /
Appendix 14) rather than a paired within-study design.  Each row of
\code{data} is one study with a single 2x2 table; the \code{test_var} column
carries the test type and must have exactly two levels.  The pipeline
is the same as \code{dta_pairwise()}:
\enumerate{
\item \code{dta_reshape()} (carrying \code{test_var} as \code{extra})
\item \code{dta_fit_pairwise()} -> nested LR-test models
\item \code{dta_compare()}      -> LR tests + Se/Sp differences
\item \code{dta_fit_single()} per arm, keyed by the two \code{test_var} levels.
}
}
\examples{
data(anti_ccp1); data(anti_ccp2)
d <- rbind(anti_ccp1, anti_ccp2)          # between-study covariate design
res <- dta_compare_tests(d, test_var = "test")
print(res)
}
