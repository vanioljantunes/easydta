% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{dta_fit_single}
\alias{dta_fit_single}
\title{Fit the bivariate binomial GLMM for a single diagnostic test}
\usage{
dta_fit_single(long, nAGQ = 1L, wide = FALSE, conf = 0.95, ...)
}
\arguments{
\item{long}{Long-format data frame produced by \code{dta_reshape()}, OR a
wide data frame -- in which case \code{wide = TRUE} will call \code{dta_reshape()}.}

\item{nAGQ}{Integer. 1 = Laplace (default, fastest, matches the Handbook
baseline). Increase (e.g. 5, 7) for adaptive Gauss-Hermite quadrature.}

\item{wide}{Logical. If TRUE, \code{long} is actually wide and will be
reshaped first.}

\item{conf}{Confidence level used for the prediction-region ellipse
computed as part of the heterogeneity summary (default 0.95).}

\item{...}{Forwarded to \code{dta_reshape()} when \code{wide = TRUE}.}
}
\value{
An S3 object of class \code{"dta_single"} (a plain list). The object
carries \verb{$heterogeneity}, a list with \code{tau_sens}, \code{tau_spec}, \code{rho},
\code{I2_sens}, \code{I2_spec}, \code{I2_biv} (Zhou-Dendukuri bivariate I^2), and
\code{pred_region} (logit-scale ellipse). \code{print(fit)} shows it.
}
\description{
Implements Cochrane Handbook Appendix 5 (Takwoingi et al., 2023):
}
\details{
glmer(cbind(true, n - true) ~ 0 + sens + spec +
(0 + sens + spec | study_id),
data = long, family = binomial, nAGQ = 1)
}
\examples{
data(anti_ccp2)
fit <- dta_fit_single(anti_ccp2, wide = TRUE)
print(fit)
}
