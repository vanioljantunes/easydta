% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_pair.R
\name{dta_forest_pair}
\alias{dta_forest_pair}
\title{Paired forest plot of the difference between two tests}
\usage{
dta_forest_pair(
  x,
  arm.e = NULL,
  arm.c = NULL,
  test.label.e = NULL,
  test.label.c = NULL,
  which = c("sens", "spec"),
  conf = 0.95,
  digits = 2,
  just = c("left", "center", "right"),
  counts = TRUE,
  widths = c(1.6, 0.4, 0.4, 0.5, 1.15, 0.4, 0.4, 0.5, 1.15, 1.8, 1.4),
  title = NULL,
  legend = NULL,
  measure_label = c("left", "forest"),
  border = FALSE
)
}
\arguments{
\item{x}{A \code{dta_pairwise_result} (from \code{dta_pairwise()} or
\code{dta_compare_tests()}).}

\item{arm.e, arm.c}{Arm names; default to the intervention / control roles
recorded on \code{x}.}

\item{test.label.e, test.label.c}{Column headings for the two arms; default
to the arm names.}

\item{which}{Which measures to draw, in order: \code{"sens"}, \code{"spec"}, or
both (the default, sensitivity above specificity).}

\item{conf}{Confidence level (default 0.95).}

\item{digits}{Display digits for the value columns (default 2).}

\item{just}{Horizontal justification of the numeric value columns
(default \code{"left"}, which keeps each column tight against the one before it).}

\item{counts}{Draw the two count columns of each arm (\code{TP} and \code{FN} in the
sensitivity block, \code{TN} and \code{FP} in the specificity block). Default \code{TRUE}.}

\item{widths}{Relative widths of the eleven columns: study labels, then for
each arm its two counts, its estimate and its confidence interval, then the
difference plot and the difference values. Defaults to
\code{c(1.6, 0.4, 0.4, 0.5, 1.15, 0.4, 0.4, 0.5, 1.15, 1.8, 1.4)}. When
\code{counts = FALSE} the four count columns are dropped.}

\item{title}{Optional plot title, rendered left-aligned above the blocks.}

\item{legend}{Optional legend text shown left-aligned below the blocks.
When \code{NULL} a one-line note on the interval method is written for you;
pass \code{NA} to suppress it.}

\item{measure_label}{Where the block titles ("Sensitivity", "Specificity")
are drawn: \code{"left"} (the default) puts them on their own line above the
block; \code{"forest"} puts them inside the CI panel, above the reference
line.}

\item{border}{Draw a rectangle around each measure block (default
\code{FALSE}).}
}
\value{
A gtable object, drawn on the current device.
}
\description{
For every study that contributes both tests, plots the difference in
sensitivity (and/or specificity) between the intervention arm (\code{.e}) and
the control arm (\code{.c}), with each arm's own estimate shown as a text
column.  By default sensitivity and specificity are drawn as two blocks in
a single figure, sensitivity on top.
}
\details{
Per-study differences carry a Newcombe hybrid-score interval computed as if
the two arms were independent samples; the discordance counts needed for a
properly paired interval are not recoverable from two marginal 2x2 tables,
so these intervals are mildly conservative.  The summary row is the
model-based paired difference from the bivariate meta-regression
(\code{x$compare$differences}) with the likelihood-ratio p-value from
\code{x$compare$lr_tests}, exactly as reported by \code{dta_sroc_pair()}.
}
\examples{
\donttest{
data(schuetz)
res <- dta_pairwise(schuetz, studlab = "studlab",
                    intervention.label = "CT", control.label = "MRI")
dta_forest_pair(res)
}
}
