% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{dta_reshape_pairwise}
\alias{dta_reshape_pairwise}
\title{Reshape paired-design wide data (intervention vs control) into long form}
\usage{
dta_reshape_pairwise(
  data,
  author = "author",
  year = "year",
  intervention = "Intervention",
  control = "Control",
  tp.e = "TP.e",
  fp.e = "FP.e",
  fn.e = "FN.e",
  tn.e = "TN.e",
  tp.c = "TP.c",
  fp.c = "FP.c",
  fn.c = "FN.c",
  tn.c = "TN.c",
  studlab = NULL,
  test_var = "test"
)
}
\arguments{
\item{data}{Data frame with one row per study.}

\item{author}{Column name of the first-author label.}

\item{year}{Column name of the publication year.}

\item{intervention}{Label to assign to the \code{.e} rows (e.g. "CT").}

\item{control}{Label to assign to the \code{.c} rows (e.g. "MRI").}

\item{tp.e, fp.e, fn.e, tn.e}{Experimental-arm 2x2 column names.}

\item{tp.c, fp.c, fn.c, tn.c}{Control-arm 2x2 column names.}

\item{studlab}{Optional: column to use as the study label instead
of \code{paste(author, year)}.  If NULL (default), the studlab is built as
\code{paste(data[[author]], data[[year]])}.}

\item{test_var}{Name for the stacked test-type column (default
\code{"test"}).  Pass this same name to \code{dta_fit_pairwise(test_var = ...)}.}
}
\value{
Long-format data frame with columns: studlab, <test_var>,
sens, spec, true, n.
}
\description{
Accepts one row per study with both arms of a paired comparison side by
side (the \code{.e} suffix for the experimental / intervention arm, \code{.c} for
the control arm) and stacks them into the Cochrane long format with
a \code{test} column carrying the two arm labels.
}
\examples{
data(schuetz)
long <- dta_reshape_pairwise(schuetz, studlab = "studlab",
                             intervention = "CT", control = "MRI")
head(long)
}
