% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.R
\name{dta_forest}
\alias{dta_forest}
\title{Coupled sensitivity / specificity forest plot}
\usage{
dta_forest(
  fit,
  test,
  conf = 0.95,
  digits = 2,
  just = c("center", "left", "right"),
  title = NULL,
  legend = NULL,
  subgroup = NULL,
  test.subgroup = TRUE,
  columns = NULL,
  overall = TRUE
)
}
\arguments{
\item{fit}{Either a \code{dta_single} object, or a \code{dta_pairwise_result}
(from \code{dta_pairwise()} / \code{dta_compare_tests()}) -- in which case \code{test}
selects which arm to plot.  The 2x2 counts and the study labels are
pulled from \code{fit$long}, so no separate data frame is needed.}

\item{test}{Required when \code{fit} is a \code{dta_pairwise_result}: which arm to
plot, given as the role \code{intervention} or \code{control} (a bare word or a
string); the arm name is looked up from the result.  Ignored otherwise.}

\item{conf}{Confidence level (default 0.95).}

\item{digits}{Numeric display digits for the value columns (default 2).}

\item{just}{Horizontal justification of the numeric value columns
(Sens/Spec "est (lci-uci)" and their headers): \code{"center"} (default),
\code{"left"}, or \code{"right"}. Centring places each value in the middle of
its own column.}

\item{title}{Optional plot title, rendered left-aligned above the plot.}

\item{legend}{Optional legend text shown left-aligned below the plot.
A character vector is rendered one line per element (similar to an
"add row" of free text).}

\item{subgroup}{Optional study-level grouping. Either a named vector
(names = study labels, values = subgroup) or an unnamed vector in the
order of the studies in \code{fit}. Studies are listed under a heading per
subgroup, in order of first appearance, each subgroup with at least two
studies closed by its own pooled bivariate estimate (italic diamond row),
and the overall estimate last.}

\item{test.subgroup}{Logical. With exactly two subgroups of at least two
studies each, print the likelihood-ratio test for a difference in
sensitivity and specificity between them (bivariate meta-regression,
via \code{\link[=dta_compare_tests]{dta_compare_tests()}}) under the I-squared row. Default \code{TRUE}.}

\item{columns}{Optional data frame of extra per-study columns drawn
between the study label and the 2x2 counts (for example method, test
type and cutoff). One row per study, matched by a \code{studlab} column when
present, otherwise in the order of the studies in \code{fit}. Column names
are the headers. A column named \code{Study} replaces the displayed label.}

\item{overall}{Logical. \code{FALSE} drops the overall pooled row, its
reference line and the I-squared row, for forests whose subgroups
should not be pooled together. Default \code{TRUE}.}
}
\value{
A gtable object drawn on the current device.
}
\description{
Coupled sensitivity / specificity forest plot
}
\examples{
data(anti_ccp2)
fit <- dta_fit_single(anti_ccp2, wide = TRUE)
dta_forest(fit)
}
