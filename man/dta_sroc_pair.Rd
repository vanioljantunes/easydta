% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sroc.R
\name{dta_sroc_pair}
\alias{dta_sroc_pair}
\title{Side-by-side SROC plot for a pairwise / covariate comparison}
\usage{
dta_sroc_pair(
  x,
  arm.e,
  arm.c,
  test.e = arm.e,
  test.c = arm.c,
  outcome = "outcome",
  population = "population",
  table = TRUE,
  auc_ic = TRUE,
  B = 2000,
  conf = 0.95,
  ncol = 2,
  ...
)
}
\arguments{
\item{x}{A \code{dta_pairwise_result} (output of
\code{dta_compare_tests()}) \emph{or} a named list of two \code{dta_single} fits.}

\item{arm.e, arm.c}{Names of the intervention (\code{.e}) and control (\code{.c})
arms in \code{x$arms} (or in \code{x} if a list was passed).}

\item{test.e, test.c}{Test labels shown in each panel title and in the
differences table header.  Default to \code{arm.e} / \code{arm.c}.}

\item{outcome, population}{Shared title fields passed through to
\code{dta_sroc()}.}

\item{table}{Logical. Show the differences table below the panels?
(default \code{TRUE}).}

\item{auc_ic}{Logical. Compute AUC point estimates + confidence
intervals + dAUC inference?  (default \code{TRUE}.)  When \code{FALSE}:
per-panel summary boxes show just the AUC point estimate (no CI),
and the differences table omits the AUC row entirely -- skipping
the bootstrap is much faster and useful for previewing.}

\item{B}{Bootstrap replicates for \code{AUC_comparison()} (default
2000); ignored when \code{auc_ic = FALSE}.}

\item{conf}{Confidence level (default 0.95).}

\item{ncol}{Number of columns for the SROC panel row (default 2).}

\item{...}{Extra arguments forwarded to both \code{dta_sroc()} calls
(e.g. \code{auc_method}, \code{labels}, \code{pred}).  \code{auc_ci} is set
automatically from \code{auc_ic} and should not be overridden.}
}
\value{
A \code{gtable} produced by \code{gridExtra::arrangeGrob()}; it prints
itself via \code{grid::grid.draw()} when sent to a graphics device.
\code{attr(., "panels")} holds the underlying ggplot list and
\code{attr(., "diff_table")} holds the rendered data.frame.
}
\description{
Convenience wrapper around two \code{dta_sroc()} calls that arranges the
intervention (\code{.e}) and control (\code{.c}) arms next to each other and,
below the panels, prints a small Cochrane-style differences table:
for each measure (Sens, Spec, AUC) the per-arm estimate (95\% CI),
the absolute difference \code{.e - .c} (95\% CI), and a p-value.
}
\details{
Statistical inference follows the Cochrane Handbook for DTA Reviews
(Appendix 12, Takwoingi et al. 2023):
\itemize{
\item Sens / Spec p-values come from likelihood-ratio tests of the
bivariate model with the relevant fixed effect constrained equal
between arms (rows 2 and 3 of \code{x$compare$lr_tests}); the absolute
difference (with delta-method CI) comes from \code{x$compare$differences}.
\item AUC inference (when \code{auc_ic = TRUE}) uses
\code{dmetatools::AUC_comparison()} (Noma & Matsushima 2020): a
parametric bootstrap that jointly returns each arm's AUC + 95\% CI,
the difference dAUC + 95\% CI, and a p-value for \code{dAUC = 0}.  When
\code{dmetatools} is not installed, AUCs are taken from \code{dta_sroc()} and
the p-value is reported as \code{n/a}.
}
}
\examples{
\donttest{
data(schuetz)
res <- dta_pairwise(schuetz, studlab = "studlab",
                    intervention = "CT", control = "MRI")
dta_sroc_pair(res, arm.e = "CT", arm.c = "MRI", auc_ic = FALSE)
}
}
