% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{dta_loo}
\alias{dta_loo}
\title{Leave-one-out sensitivity analysis}
\usage{
dta_loo(x, conf = 0.95, auc_ci = TRUE, B = 2000, verbose = FALSE)
}
\arguments{
\item{x}{A \code{dta_single} fit or a \code{dta_pairwise_result} (from
\code{\link[=dta_pairwise]{dta_pairwise()}} / \code{\link[=dta_compare_tests]{dta_compare_tests()}}).  In a paired design both
arms of the omitted study are dropped together.}

\item{conf}{Confidence level (default 0.95).}

\item{auc_ci}{Logical. Bootstrap a CI for the AUC (and, for a
comparison, the dAUC CI and p-value)?  Default \code{TRUE}; \code{FALSE} keeps
only the AUC point estimates and is much faster.}

\item{B}{MVN-bootstrap replicates per refit (default 2000).}

\item{verbose}{Logical. Print one line per refit (default \code{FALSE}).}
}
\value{
An S3 object of class \code{"dta_loo"} with \verb{$table} (one row per
omitted study plus the pooled row, flagged by \verb{$table$pooled}),
\verb{$type} (\code{"single"} or \code{"pair"}), \verb{$letters} (the row codes used by
the plot) and the sROC geometry of every refit.  \code{print()} shows the
table; \code{plot()} / \code{\link[=dta_loo_forest]{dta_loo_forest()}} draws the forest and sROC figure.
}
\description{
Refits the model once per study with that study removed and collects
the pooled measures each time, like \code{meta::metainf()}.  For a single
test the table holds sensitivity, specificity, AUC, LR+ and LR- (each
with a confidence interval).  For a pairwise comparison it holds the
difference \code{.e - .c} in each of those measures with its confidence
interval and p-value.  The last row is the estimate from all studies.
}
\examples{
\donttest{
data(anti_ccp2)
fit <- dta_fit_single(anti_ccp2, wide = TRUE)
loo <- dta_loo(fit, auc_ci = FALSE)
print(loo)
plot(loo)
}
}
