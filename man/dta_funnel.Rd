% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funnel.R
\name{dta_funnel}
\alias{dta_funnel}
\title{Deeks funnel plot + Deeks test for asymmetry (DOR scale)}
\usage{
dta_funnel(
  fit,
  arm = NULL,
  test = "test",
  outcome = "outcome",
  population = "population",
  continuity = 0.5,
  conf = 0.95,
  table = TRUE,
  ...
)
}
\arguments{
\item{fit}{Either a \code{dta_single} object or a \code{dta_pairwise_result}
(output of \code{dta_compare_tests()}); when pairwise, pass \code{arm} to
choose which test arm to plot.}

\item{arm}{Required when \code{fit} is a \code{dta_pairwise_result}: name of
the arm in \code{fit$arms} to plot.  Ignored otherwise.}

\item{test, outcome, population}{Title fields ("Deeks funnel of \if{html}{\out{<test>}}
to predict \if{html}{\out{<outcome>}} in \if{html}{\out{<population>}}").}

\item{continuity}{Continuity correction added to all four 2x2 cells of
any study with a zero cell (default \code{0.5}).}

\item{conf}{Confidence level for the pooled DOR CI (default 0.95).}

\item{table}{Logical -- show the Deeks results table below the funnel?
(default \code{TRUE}).}

\item{...}{Reserved.}
}
\value{
A \code{gtable} produced by \code{gridExtra::arrangeGrob()}; printed via
\code{grid::grid.draw()} when sent to a graphics device.  The Deeks
regression result is on \code{attr(., "deeks")} and the per-study data
on \code{attr(., "study_data")}.
}
\description{
Cochrane-recommended publication-bias diagnostic for DTA meta-analysis
(Handbook v2.0 ch. 10.6.4; Deeks, Macaskill & Irwig 2005).  Produces a
funnel plot of \code{ln(DOR)} against \code{1/sqrt(ESS)} with the REML pooled
\code{ln(DOR)} as the reference line, and below it a small results table
from the Deeks weighted linear regression test.
}
\examples{
data(anti_ccp2)
fit <- dta_fit_single(anti_ccp2, wide = TRUE)
dta_funnel(fit, test = "anti-CCP2",
           outcome = "rheumatoid arthritis", population = "adults")
}
