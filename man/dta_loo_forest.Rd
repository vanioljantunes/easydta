% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{dta_loo_forest}
\alias{dta_loo_forest}
\title{Leave-one-out forest plot with sROC curves}
\usage{
dta_loo_forest(
  x,
  sroc = TRUE,
  digits = 2,
  title = NULL,
  row_in = 0.2,
  sroc_in = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{dta_loo} object, or a \code{dta_single} /
\code{dta_pairwise_result} (then \code{\link[=dta_loo]{dta_loo()}} is run first; \code{...} is
forwarded to it).}

\item{sroc}{Logical. Draw the sROC panel below the forest?
Default \code{TRUE}.}

\item{digits}{Display digits for the value columns (default 2).}

\item{title}{Optional title, left-aligned above the forest.}

\item{row_in}{Height of one forest row in inches (default 0.2).}

\item{sroc_in}{Height of the sROC band in inches (default 4 for one
panel or two).}

\item{...}{Forwarded to \code{\link[=dta_loo]{dta_loo()}} when \code{x} is not a \code{dta_loo}.}
}
\value{
A \code{gtable} of class \code{"dta_loo_plot"}; its \code{print()} method draws
it, so it renders when returned at top level.
}
\description{
Draws the leave-one-out table of \code{\link[=dta_loo]{dta_loo()}} as a forest plot in the
style of \code{meta::forest(metainf())}: one row per omitted study
("Omitting ..."), the pooled estimate last, a letter code on every row.
For a single test the sensitivity and specificity carry the CI panels
and AUC, LR+ and LR- are text columns; for a comparison the
differences in sensitivity and specificity carry the CI panels and each
difference is followed by its p-value.
}
\details{
Beneath the forest, an unlabelled sROC panel (two for a comparison, one
per arm) draws one thin coloured curve per omission and the full-data
curve in black.  Each study point is drawn as its row letter, in the
colour of the curve fitted without that study, so an influential study
is read off directly: its letter sits away from the cloud and its curve
departs from the black one.
}
\examples{
\donttest{
data(anti_ccp2)
fit <- dta_fit_single(anti_ccp2, wide = TRUE)
dta_loo_forest(fit, auc_ci = FALSE)
}
}
