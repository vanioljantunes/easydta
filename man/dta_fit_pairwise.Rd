% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{dta_fit_pairwise}
\alias{dta_fit_pairwise}
\title{Fit the bivariate meta-regression for pairwise test comparison}
\usage{
dta_fit_pairwise(
  long,
  test_var,
  variance = c("equal", "unequal"),
  nAGQ = 1L,
  wide = FALSE,
  ...
)
}
\arguments{
\item{long}{Long-format data frame from \code{dta_reshape()} (with
\code{extra} including the test-type column) OR a wide data frame when
\code{wide = TRUE}.}

\item{test_var}{Name (string) of the test-type column -- must have
exactly two distinct non-NA values.}

\item{variance}{Random-effects structure: \code{"equal"} (default, model B,
shared between-study variances) or \code{"unequal"} (model E, separate
per-test variances).}

\item{nAGQ}{Integer; Laplace by default.}

\item{wide}{Logical. If TRUE, reshape first.}

\item{...}{Forwarded to \code{dta_reshape()} when \code{wide = TRUE}.}
}
\value{
An S3 object of class \code{"dta_pairwise"}.
}
\description{
Fits four nested models in the order recommended by Cochrane Handbook
Appendix 12 (p.56-57):
}
\details{
A. null:                0 + sens + spec
B. both-vary (full):    0 + seA + seB + spA + spB
C. spec-varies only:    0 + sens + spA + spB
D. sens-varies only:    0 + seA + seB + spec

The random-effects structure is controlled by \code{variance}:
\itemize{
\item \code{"equal"}   (default, Cochrane model B): a single shared block
\code{(0 + sens + spec | study_id)} in every model.
\item \code{"unequal"} (Cochrane model E): two per-test blocks
\code{(0 + seA + spA | study_id) + (0 + seB + spB | study_id)}, allowing
each test its own between-study variances and covariance.
}

The chosen structure is applied to all four nested models so the
likelihood-ratio ladder stays nested.

The user-supplied \code{test_var} is coerced to a two-level factor; the first
level (alphabetical) becomes "A", the second "B".
}
\examples{
data(schuetz)
long <- dta_reshape_pairwise(schuetz, studlab = "studlab",
                             intervention = "CT", control = "MRI")
pair <- dta_fit_pairwise(long, test_var = "test")
}
